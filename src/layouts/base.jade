//- Mixin for post, used on homepage and post page
mixin postHead(post)
  .post-head.group
    a(href="#{data.baseUrl}#{post.url}")
      h1.post-title= post.title
    span.post-date!= 'Published ' + post.date.toLocaleDateString().split(',').slice(1).join(',')

mixin post(post)
  mixin postHead(post)
  .post-body.markdown!= post.content

doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="icon", type="image/png", href="#{data.baseUrl}images/cabin.png")
    link(href="#{data.baseUrl}styles/main.css", rel="stylesheet")
    block head
      title= "thealphanerd.io"
  body
    #big-guy.light-grey
    #medium-guy.dark-grey
    #tiny-guy.almost-black
    

    
    //- #middle
      
    .center
      nav.group
        h1.name
          a(href="#{data.baseUrl}") Myles Borins
        ul.nav-links
          li.text-link
            a(href="#{data.baseUrl}about.html") about
          li.text-link
            a(href="#{data.baseUrl}projects.html") projects
          li.text-link
            a(href="#{data.baseUrl}archives.html") archives
      block content
      
    #footer
      #tiny-guy.almost-black
      #medium-guy.dark-grey
      #big-guy.light-grey
      .center
        p
          | Welcome to thealphanerd.io