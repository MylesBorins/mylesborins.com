extends ../layouts/base

block seo
  link(rel="canonical", href="https://mylesborins.com/talks.html")

block head
  title= "MylesBorins.com | talks"

block content
  #talks
    each post in posts
      if(post.type === "talks")
        .post-body.markdown!= post.content
