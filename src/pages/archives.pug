extends ../layouts/base

block seo
  link(rel="canonical", href="https://mylesborins.com/archives.html")

block head
  title= "MylesBorins.com | archives"

block content
  each post in posts
    if(post.type === "blog")
      +postHead(post)
